Algoritmo "Ex_18_Lista_1"

Var
   nu: inteiro
   res: real

Inicio
      escreval("Digite um numero:")
         leia(nu)
        res <- nu%2 // operador de modulo "%" = calculo de resto de divisao
      escreval("O resto de ", nu," dividido por 2 e", res)

Fimalgoritmo